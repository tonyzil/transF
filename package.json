{
  "name": "transf",
  "version": "0.1.0",
  "private": true,
  "description": "On-chain remittance platform \u2014 Phase 0 corridor MVP (EUR in via IBAN, cash pickup out)",
  "type": "module",
  "scripts": {
    "dev": "tsx scripts/dev.ts",
    "chain": "hardhat node",
    "compile": "hardhat compile",
    "deploy": "tsx scripts/deploy.ts",
    "api": "tsx services/api/src/server.ts",
    "test:contracts": "tsx contracts/test/run.ts",
    "monerium:check": "tsx scripts/monerium-check.ts",
    "stellar:check": "tsx scripts/stellar-check.ts",
    "cctp:dryrun": "tsx scripts/cctp-dryrun.ts",
    "e2e": "tsx scripts/e2e.ts",
    "webauthn:selftest": "tsx scripts/webauthn-selftest.ts",
    "fp3:test": "tsx scripts/fp3-test.ts",
    "audit:deps": "npm audit",
    "check": "npm run compile && npm run test:contracts && npm run webauthn:selftest && npm run fp3:test && npm run e2e"
  },
  "devDependencies": {
    "hardhat": "^2.26.0",
    "tsx": "^4.19.0",
    "typescript": "^5.5.0"
  },
  "dependencies": {
    "@stellar/stellar-sdk": "^16.0.1",
    "abstractionkit": "^0.4.0",
    "express": "^4.19.0",
    "viem": "^2.21.0"
  }
}
